<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cs410_hci.holgater.cs410finalproject">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ProductListMenu">
        </activity>
        <activity
            android:name=".ProductEditMenu"
            android:label="@string/title_activity_item_edit_menu" />
        <activity android:name=".ComponentListMenu" />
        <activity android:name=".ComponentEditMenu" />
        <activity
            android:name=".AddProduct"
            android:label=""
            android:theme="@style/AppTheme" />
        <activity android:name=".AddComponent" />
        <activity
            android:name=".RecipeItemSelect"
            android:label="@string/title_activity_recipe_item_select"
            android:theme="@style/AppTheme" />
        <activity android:name=".Load_Items">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>